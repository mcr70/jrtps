Maven dependency

 To get starting with developing against jRTPS, add this dependency your pom.xml

+-----
<dependency>
    <groupId>${project.groupId}</groupId>
    <artifactId>${project.artifactId}</artifactId>
    <versio>${project.version}</version>
<dependency>
+-----

 jRTPS itself depends only on slf4j.api. Alternatively, you may download jRTPS from
 {{http://maven.org}}
